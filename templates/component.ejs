import React from 'react';
import glamorous from 'glamorous-native';
import PropTypes from 'prop-types';

const <%= props.name %> = glamorous.view(
  {},
  props => ({

  })
);

<%= props.name %>.propTypes = {};

// <%= props.name %>.defaultProps = {};

export default <%= props.name %>
